#pragma once
#include <vector>

using namespace std;

int* varaa_taulukko(int n);
void lue_arvot(int*, const vector<int>&);
void kaanna(int*, const vector<int>&);
void tulosta_arvot(const int*, const vector<int>&);
